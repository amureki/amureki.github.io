{
  "name": "amureki.me",
  "scripts": {
    "build": "NODE_ENV=production npx tailwindcss -i styles/styles.css -o static/styles.css",
    "watch": "npx tailwindcss -i styles/styles.css -o static/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "tailwindcss": "^3.4.14"
  }
}
