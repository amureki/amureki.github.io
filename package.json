{
  "name": "amureki.me",
  "scripts": {
    "build": "NODE_ENV=production npx @tailwindcss/cli -i styles/styles.css -o static/styles.css -m",
    "watch": "npx @tailwindcss/cli -i styles/styles.css -o static/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.1.13"
  }
}
